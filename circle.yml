dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh

deployment:
  production:
    branch: "master"
    commands:
      - printf "dexterneo\nfbicia131289\n" | meteor deploy eklorefete.meteor.com