dependencies:
  cache_directories:
    - ~/.meteor
  override:
    - meteor || curl https://install.meteor.com | /bin/sh
    - npm install -g eslint@2.8.0

test:
  override:
    - eslint client/ imports/ server/
